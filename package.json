{
  "name": "sokkaverse", // Adjusted name to match repo
  "version": "0.0.0",
  "private": true,
  "homepage": "https://sokkastark.github.io/sokkaverse", // <--- IMPORTANT: Matches your new repo
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "framer-motion": "^11.2.11",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "gh-pages": "^5.0.0",
    "vite": "^5.0.8"
  }
}